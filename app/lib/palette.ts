// Single TypeScript source for the brand colours, used wherever CSS custom
// properties can't reach (SVG plate strokes on differently-coloured sections,
// the OG image renderer). globals.css declares the same values as --ink,
// --orange and --bone-on-dark: keep the two files in sync.
export const INK = "#0C0B0A";
export const ORANGE = "#FF6737";
export const BONE = "#F6EDE7";
